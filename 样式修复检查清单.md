# 样式修复检查清单

## 🔍 问题诊断

页面显示混乱的原因：**CSS 和 JavaScript 文件没有正确加载**

---

## ✅ 需要完成的步骤

### 步骤 1：上传 CSS 和 JS 文件到服务器

在 FileZilla 中，将以下文件上传到主题目录：

**上传位置**：
```
/domains/你的域名/public_html/wp-content/themes/blocksy-child/assets/css/warm-earth-home.css
/domains/你的域名/public_html/wp-content/themes/blocksy-child/assets/js/warm-earth-home.js
```

**操作步骤**：
1. 在 FileZilla 左侧（本地），找到：
   - `assets/css/warm-earth-home.css`
   - `assets/js/warm-earth-home.js`
2. 在 FileZilla 右侧（远程），导航到：
   - `domains` → 你的域名 → `public_html` → `wp-content` → `themes` → `blocksy-child`
3. 如果 `assets` 文件夹不存在，先创建它（右键 → 创建目录 → 输入 `assets`）
4. 在 `assets` 文件夹中创建 `css` 和 `js` 文件夹
5. 上传文件到对应文件夹

---

### 步骤 2：确保 functions-weh-enhancements.php 已上传

**上传位置**：
```
/domains/你的域名/public_html/wp-content/themes/blocksy-child/functions-weh-enhancements.php
```

**操作步骤**：
1. 在 FileZilla 左侧找到 `functions-weh-enhancements.php`
2. 上传到 `blocksy-child` 文件夹根目录

---

### 步骤 3：在主题的 functions.php 中引入文件

**方法 A：通过 WordPress 后台（推荐）**

1. 登录 WordPress 后台
2. 进入 **外观** → **主题文件编辑器**
3. 选择 **blocksy-child** 主题
4. 选择 **functions.php** 文件
5. 在文件**末尾**添加以下代码：

```php
// Warm Earth Home Enhancements
require_once get_stylesheet_directory() . '/functions-weh-enhancements.php';
```

6. 点击 **更新文件**

**方法 B：通过 FileZilla**

1. 在 FileZilla 中找到：
   ```
   /domains/你的域名/public_html/wp-content/themes/blocksy-child/functions.php
   ```
2. 下载到本地备份
3. 在文件末尾添加：
   ```php
   // Warm Earth Home Enhancements
   require_once get_stylesheet_directory() . '/functions-weh-enhancements.php';
   ```
4. 上传回服务器

---

### 步骤 4：清除缓存

1. 在 WordPress 后台，找到 **LiteSpeed Cache** 插件
2. 点击 **清除所有缓存**
3. 或者使用浏览器清除缓存（Ctrl+Shift+Delete）

---

### 步骤 5：验证文件加载

1. 访问：https://warmearthhome.com/support-center/
2. 按 **F12** 打开浏览器开发者工具
3. 切换到 **Network（网络）** 标签
4. 刷新页面（F5）
5. 查找以下文件是否加载成功：
   - `warm-earth-home.css` - 状态应该是 200
   - `warm-earth-home.js` - 状态应该是 200
   - Google Fonts（Playfair Display, Inter）

**如果文件返回 404 错误**：
- 检查文件路径是否正确
- 确认文件已上传到正确位置
- 检查文件权限（应该是 644）

---

## 🎯 预期效果

修复后，页面应该显示：
- ✅ 美观的字体（Playfair Display 标题，Inter 正文）
- ✅ 正确的颜色（Warm Sand 背景，Deep Clay 按钮）
- ✅ 整齐的布局（卡片、间距、对齐）
- ✅ 响应式设计（移动端自适应）
- ✅ 交互效果（FAQ 展开/收起、标签切换等）

---

## 🆘 如果还有问题

### 问题 1：CSS 文件 404 错误

**检查**：
- 文件路径：`blocksy-child/assets/css/warm-earth-home.css`
- 文件名大小写是否正确
- 文件权限是否为 644

### 问题 2：样式仍然混乱

**检查**：
- `functions-weh-enhancements.php` 是否已引入到 `functions.php`
- 清除所有缓存（WordPress + 浏览器）
- 检查浏览器控制台是否有 JavaScript 错误

### 问题 3：字体没有加载

**检查**：
- 网络连接是否正常
- Google Fonts 是否被防火墙阻止
- 检查 `functions-weh-enhancements.php` 中的字体加载代码

---

## 📝 文件结构检查清单

确保以下文件结构存在：

```
blocksy-child/
├── functions.php                    ← 需要添加引入代码
├── functions-weh-enhancements.php   ← 已上传
├── page-support.php                 ← 已上传
├── assets/
│   ├── css/
│   │   └── warm-earth-home.css     ← 需要上传
│   └── js/
│       └── warm-earth-home.js      ← 需要上传
```

---

完成以上步骤后，页面样式应该恢复正常！








